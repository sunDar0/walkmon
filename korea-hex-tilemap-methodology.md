# walkmon 지형 타일 방법론 (한국 헥사 바이옴 레이어)

> 대한민국 지형을 육각(hex) 타일로 분류·표현하는 작업의 방법론이다.
> **walkmon(포켓 랜드) 기획서 최신본**에 맞춰 갱신했으며, 새 세션에서 이 문서 하나로 전체 접근을 파악할 수 있게 작성했다.
> (그리드가 50m·S2 → H3 Level 9 → **H3 Level 11**로 바뀌어 왔음. 현재 기준은 Level 11.)

---

## 0. 이 방법론의 역할

전국을 한 장으로 그리는 것이 아니라, **각 H3 셀이 어떤 지형(바이옴)인지 판정하는 '바이옴 데이터 레이어'**를 만드는 것이 본질이다.

이 레이어가 쓰이는 곳:

- **점령 타일 렌더링**: 유저가 밟아 공개된 셀을 해당 바이옴 도트 타일로 표시.
- **지역 특산 아이템 드롭**: 한강 통과 → 라면, 산악 → 나물처럼 **바이옴이 곧 드롭 테이블 키**(기획서 3.2).
- **전체 지도 뷰(선택·미구현)**: 안개로 가린 전국 위에 공개율을 보여주는 개요 지도.

실시간 게임 화면과는 별개다. 실시간 화면은 H3 + 시야 링 + 캐릭터 중심 고정 카메라 + 도트 격자가 담당한다(기획서 5장).

---

## 1. 그리드와 규모 — H3 Level 11(약 50m) 기준

이번 기획서에서 그리드가 **H3 Level 9(약 170m) → Level 11(한 칸 폭 약 50m)**로 잦아졌다. 규모 계산을 다시 한다.

- H3 Level 11: 변 길이 약 25m, 셀 면적 약 0.00215 km², **꼭짓점 폭 약 50m**.
- 남한 육지 약 10만 km² ÷ 0.00215 ≈ **약 4,700만 셀**.
- 전국을 Level 11로 통째 미리 계산하면 셀당 1바이트라도 약 47MB(압축 시 크게 줄지만 "약 1MB"는 아님).

**핵심 판단**: 전국을 Level 11로 통째 미리 계산할 필요가 없다.
게임 시야가 반경 약 150m 링뿐이라, 바이옴이 필요한 곳은 유저가 실제 가는 셀 주변뿐이다.

**권장 구조 — 코스 프리컴퓨트 + 부모 셀 상속**

- 바이옴을 **Level 8~9의 코스 레이어**로만 미리 계산한다.
  - Level 9: 약 100만 셀 → 약 1MB(앱 동봉 가능, 오프라인 강함).
  - Level 8: 약 14만 셀 → 수백 KB(더 가벼움, 대신 거칢).
- Level 11 각 셀의 바이옴은 `cellToParent`로 부모 코스 셀의 값을 상속한다.
- 이러면 **전국 어디서나 결정적(deterministic) 바이옴을 오프라인으로** 얻으면서 자산 크기는 약 1MB로 유지된다.

**단, 물/경계(강·해안·해변)는 코스 레이어로는 얇아서 놓친다** → 이 세 종류만 런타임에 물 마스크/OSM로 보정한다(5장).

---

## 2. 분류 방법과 데이터 소스

각 (코스) 셀 중심점이 실제로 어떤 땅인지 래스터 다수결로 판정한다.

- **토지피복**: 환경부 토지피복지도(국내 세분류) 또는 ESA WorldCover(10m, 무료).
- **고도·경사**(산/돌산/평지): 국토지리정보원 DEM 또는 SRTM 30m.
- **수역·해안 정밀화**: OSM(`natural=coastline`, `natural=water`, `waterway`, `natural=beach`).

처리 흐름:

1. 남한 범위를 코스 해상도(Level 8~9)로 폴리필(`polygonToCells`).
2. 각 셀에 걸치는 토지피복 픽셀을 다수결로 집계 → 대표 바이옴.
3. 고도·경사·OSM으로 세부 변형(산/돌산/도심 등) 보정.
4. `{coarse_h3_id: 바이옴코드}` 저장. 런타임에 Level 11 셀은 부모 조회로 바이옴 획득.

도구: `rasterio` / `GDAL` / `numpy` + `h3-py`(h3-js와 인덱스 호환).

---

## 3. 6종 타일 → 한국 지형 매핑 (바이옴 = 아이템 드롭 키)

> 한국엔 사막·상시 적설이 사실상 없다. 두 타일은 재해석 또는 미사용.

| 타일(바이옴)       | 매핑 기준                                         | 아이템 드롭(예시) |
| ------------------ | ------------------------------------------------- | ----------------- |
| 풀 / 들판          | 초지, 논밭                                        | 들풀·곡물         |
| 산                 | 산림 + 고도·경사 높은 지역                        | 산악 나물         |
| 흙                 | 나지·맨땅                                         | —                 |
| **사막**           | 없음 → 하천 사구/넓은 나지 재활용 또는 **미사용** | —                 |
| 모래               | 해안·하천 모래톱                                  | 조개류            |
| 물 / 강 / 바다     | 내수면 / 하천 / 해양                              | 한강 라면·수산물  |
| 강변 / 해변        | 육지–수역 경계 셀(전이, 5장)                      | 경계 특산         |
| 돌산 / 돌길 / 돌밭 | 암반 노출지 + 도심 포장(돌길=시가지/도로)         | 도심 특산         |
| **눈**             | 없음 → 계절 오버레이 / 겨울 고산 / 미사용         | 겨울 한정         |

**아트 주의**: 처음 첨부한 사실적 육각 타일과 기획서의 **16비트 도트(GBA풍)** 방향은 다르다. 분류는 아트에 독립적이므로("이 셀=산"만 판정), 사실적 타일은 **바이옴 참고용**으로 두고 최종 에셋은 도트로 재제작한다.

---

## 4. 좌표계와 격자 (H3 상태 / 도트 격자 렌더 분리)

- **게임 상태 = H3 Level 11**(위경도 위 직접 인덱싱, 런타임 투영 불필요).
- **렌더 = 북쪽 정렬 pointy-top 도트 육각 격자.** H3 셀을 그대로 투영하면 위치마다 기울어 픽셀 스프라이트와 안 맞으므로, **깔끔한 격자를 깔고 각 칸 중심을 H3 셀에 역매핑**한다.
- **삼각형 그물망(Mesh)은 폐기**(초기 검토안). 지난 문서의 삼각형 서술은 무효.
- **투영(EPSG:5179)은 전체 지도 뷰/포스터 이미지를 뽑을 때만** 필요. 게임 런타임에는 불필요.
- **행정구역 스코어(추가 예정)**: 바이옴과 별개 레이어로, 역지오코딩(카카오/네이버)으로 행정동을 붙인다.

---

## 5. 엣지 타일(강변·해변) 오토타일링

- 강변·해변은 물/육지가 비스듬히 가로지르는 방향성 전이 타일이다.
- 렌더 셀의 H3 이웃 6방향(`gridDisk` k=1)이 물인지 육지인지 **비트마스크로 계산** → 해당 회전각의 타일 선택. 회전은 렌더 시점 적용으로 원본 수 절약.
- **물/강/해안/해변은 코스 바이옴 레이어로는 얇아 놓치므로**, 이 판정은 런타임에 물 마스크(또는 OSM 해안·수역)로 별도 수행한다.

---

## 6. DMZ 클리핑 — 형태 관련 필수 처리

- 해안선은 육지/바다 분류에서 저절로 반도 윤곽으로 나온다.
- 그러나 남북 경계(휴전선)는 정치적 선이라 토지피복은 여기서 멈추지 않는다.
- **남한 행정경계 폴리곤으로 격자를 클리핑(마스킹)** 해 경계 밖 셀을 제외한다.
- 없으면 "남한 형태"가 아니라 "한반도 남부 일대"가 된다.

---

## 7. 형태 정밀도 트레이드오프

- 셀 크기는 H3 Level 11(약 50m)로 고정.
- 형태 정밀도에 영향을 주는 건 **전체 지도 뷰/포스터의 픽셀 해상도**뿐이다.
  - 크게: 해안선·섬·좁은 곶까지 충실(제주·울릉·강화, 남해 다도해).
  - 작게: 계단(quantization) 두드러지고 작은 섬 소실.
- 육각 격자는 사각 격자보다 대각선 경계가 매끄럽게 근사돼 유리.

---

## 8. 렌더링·데이터 전략 — 두 뷰 체계

화면은 크게 **플레이 뷰**와 **전체 지도 뷰** 둘로 나뉜다. 둘은 목적·해상도·데이터가 다르므로 분리해 다룬다.

**두 뷰는 버튼으로 전환하며, 줌 구간을 매끄럽게 잇지 않는다(확정).**

- 플레이 뷰 최대 줌아웃(약 1.5km)과 전체 지도 뷰 최소 줌 사이에 빈 구간이 있어도 무방하다. 각 뷰가 독립된 줌 범위를 갖는다.
- 이렇게 하면 폐기했던 무한 줌아웃·LOD(단계별 격자 전환)의 부자연스러운 이음새 문제를 애초에 만들지 않는다.
- 전환 시 카메라는 현재 위치를 기준으로 각 뷰의 기본 줌으로 스냅한다.

### 8.1 플레이 뷰 (실시간 게임 화면)

- **타일**: H3 Level 11(약 50m) 그대로. 바이옴 도트 타일을 정밀하게 렌더.
- **줌 범위**: 줌인 약 205m ~ **줌아웃 약 1.5km**(화면 폭 기준). 이 범위를 넘지 않는다.
- **렌더**: react-native-skia 도트 타일맵. nearest-neighbor 정수배 스케일로 도트 선명 유지.
- **시야 윈도우**: 현재 위치 H3 링(반경 약 150m)을 어둠 wash 경계로, 그 밖 미개척지는 생략.
- **카메라**: 캐릭터 화면 중앙 고정 + 월드 이동.
- **배경 지도**: 현재 CartoDB Voyager 래스터를 흐리게 사용, 만화풍 교체/제거 검토 중.
- **바이옴 소스**: 코스 프리컴퓨트(약 1MB) + `cellToParent` 상속 + 물/엣지 런타임 보정(2·5장).

### 8.2 전체 지도 뷰 (발자취 개요)

- **목적**: 상세 타일이 아니라 **어디를 밟았는지 발자취를 러프하게** 보여줌.
- **줌 범위**: 플레이 뷰보다 넓은 구간을 담당하며, **최대 줌아웃 시 대한민국 전체가 한 화면에** 들어옴.
- **핵심 제약**: 전국을 화면에 담으면 화면 1px가 수백 m~1km를 덮는다(남한 폭 약 300km ÷ 모바일 약 400px ≈ 약 750m/px). **50m 타일은 1px 아래로 사라지므로 그대로 못 그린다 → 집계(aggregation) 필수.**

**발자취 집계 방식 (H3 부모 롤업)**

- 저장은 플레이 뷰와 동일하게 Level 11 점령 셀(`occupied`)만 둔다.
- 렌더 시 줌 레벨에 맞는 목표 해상도로 `cellToParent`하여 "밝은 부모 셀" 집합을 만든다.
  - `lit = new Set(occupied.map(c => cellToParent(c, targetRes)))`
- 목표 해상도 대략선(셀이 화면에서 최소 몇 px 되도록):

  | 화면 폭(대략)         | 집계 해상도(대략) | 셀 크기  |
  | :-------------------- | :---------------- | :------- |
  | 전국 약 300km         | H3 Level 6        | 약 3~4km |
  | 광역 약 30~50km       | H3 Level 8        | 약 460m  |
  | 플레이 뷰 인접(수 km) | H3 Level 10       | 약 66m   |

- 점령 셀 수가 매우 많아지면 롤업 결과를 해상도별로 캐시(재계산 방지).

**배경(전국 바탕)**

- **DMZ 클리핑된 남한 윤곽**을 바탕으로 깔고, 그 위에 발자취를 얹는다(6장 클리핑이 여기서 쓰임).
- 바탕은 두 선택지: (a) 단순 남한 실루엣(가벼움) (b) **코스 바이옴 레이어를 옅게 틴트**(이미 프리컴퓨트하므로 사실상 공짜, 지형 느낌까지 전달).
- 전국 이미지를 실제 픽셀로 뽑을 때만 **EPSG:5179 평면 렌더**를 쓴다(런타임 H3와 별개).

### 8.3 공통 — 로컬 저장소

- AsyncStorage 단일 객체 `{ occupied, stageIndex, stageXp, items }`, 키 `walkmon_state_v3`.
- 바이옴 캐시가 필요하면 별도 키로 두거나 이 객체에 `biomeCache` 추가.

---

## 9. 권장 진행 순서

1. 코스 해상도(Level 8~9) 확정 + 남한 경계 클리핑 규칙 확정.
2. 한 지역(예: 판교 10×10km) 토지피복+DEM으로 바이옴 분류 → 매핑 검증.
3. 해당 지역 PIL 렌더로 바이옴·엣지 타일 눈으로 확인(디버그).
4. 규칙 확정 후 전국 코스 바이옴 레이어(`coarse_h3_id → 바이옴`) 일괄 생성.
5. 클라이언트: Level 11 셀 → 부모 조회로 바이옴 획득 + 물/엣지는 런타임 보정 + 아이템 드롭 연결.

작게 시작해 매핑 규칙부터 눈으로 확정하는 것이 시간을 가장 많이 아낀다.

---

## 10. 착수 전 의사결정 체크리스트

- [ ] 코스 바이옴 해상도: Level 8(가벼움) vs Level 9(약 1MB·더 정밀)?
- [ ] 바이옴 레이어: 앱 동봉 vs API 서빙?
- [ ] 물/강/해안 보정 소스: 자체 물 마스크 vs OSM 런타임 조회?
- [ ] 토지피복: 환경부(정밀) vs WorldCover(무료·확장)?
- [ ] 사막 타일: 재활용 vs 미사용?
- [ ] 눈 타일: 계절 오버레이 vs 겨울 고산 vs 미사용?
- [ ] 돌길을 도심/도로로 재해석?
- [ ] 남한 경계 클리핑 폴리곤 출처(행정안전부/통계청)?
- [ ] 사실적 참고 타일 → 도트 에셋 재제작 범위/일정?
- [ ] 바이옴 캐시를 `walkmon_state_v3`에 합칠지, 별도 키로 둘지?
- [ ] 전체 지도 뷰 바탕: 단순 남한 실루엣 vs 코스 바이옴 틴트?
- [ ] 전체 지도 뷰 발자취 롤업을 실시간 계산 vs 해상도별 캐시?
- [x] 플레이 뷰 ↔ 전체 지도 뷰: **버튼 전환(구간 안 이음)으로 확정.**

---

## 11. 현재 구현 상태 (기획서 최신본 기준)

- **되는 것**: 플레이어블 캐릭터가 현재 위치 타일 위에 배치되고, 이동 시 함께 움직임. GPS→H3 변환, 점령·XP·아이템 로컬 저장(AsyncStorage), 단계별 성장(알~성년)·수동 진화, skia 도트 렌더, 시야 링·고정 카메라.
- **추가 예정**: 백그라운드 위치 추적(현재 포그라운드만), 홈 위젯, 트레이너 스프라이트(현재 펫만 등장), 행정구역 매핑, 전체 지도 뷰.

> 바이옴 방법론은 위 "추가 예정"의 아이템 드롭·전체 지도 뷰와 맞물린다. MVP 단계에서는 코스 레이어 없이 소수 지역만 하드코딩 바이옴으로 시작해도 무방하며, 확장 시 본 문서 구조로 옮기면 된다.

---

## 부록: 그리드 변경 이력

| 항목             | 초기            | H3 v1                   | **현재(H3 v2)**                       |
| ---------------- | --------------- | ----------------------- | ------------------------------------- |
| 그리드           | 50m / S2 L16    | H3 Level 9(약 170m)     | **H3 Level 11(약 50m)**               |
| 전국 셀 수       | 약 5천만        | 약 100만                | **약 4,700만**                        |
| 바이옴 저장 전략 | 전국 프리컴퓨트 | 전국 프리컴퓨트(약 1MB) | **코스(L8~9) 프리컴퓨트 + 부모 상속** |
| 렌더             | 헥사 이미지     | 삼각형 메시             | **pointy-top 도트 격자(H3 역매핑)**   |
| 지도 렌더 스택   | —               | @rnmapbox/maps          | **react-native-skia**                 |
| 저장소           | —               | expo-sqlite             | **AsyncStorage(`walkmon_state_v3`)**  |
| XP(신규/재방문)  | —               | +25 / +5                | **+10 / +2** (점수 +4 / +1)           |
